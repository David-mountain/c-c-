--makefile
项目管理工具：大型工程化文件 手动编译不太现实 帮助我们编译 自动化编译 像一个shell脚本
主要解决2个问题：大量代码的关系维护 减少重复编译时间
type make 
which make 
大部分IDE都有这个工具
Makefile文件命名规则： 推荐使用Makefile 虽然makefile也可
sudo apt install make 

find ./ -name "*.c" | wc -l 
69 查看某类型文件数量  
自动化编译 好处
type make 
which make 
file /user/bin/make 
解决了什么问题？

文件命名 推荐Makefile 
sudo apt install make 

--Makefile语法规则
一条规则 两个函数 三个变量
vim 1.mk 
make -f 1.mk 
三要素：目标 依赖文件 命令

有些没懂，这一集

没有指定目标，默认第一条
可以指定

第三集，我感觉写项目之前看看，现在看后面也忘记了

